# Harness companion configuration generated by app-dev-harness.
version: 1
project:
  name: "fin"
  constraints: "- Add project-specific rules here."
stack: "node"
approvals:
  visual: required
  push: required
  deploy: required
  migration: required
  productionWrite: required
  destructive: required
deployment:
  enabled: false
paths:
  currentWork: ".harness/current-work.md"
  pitfalls: "docs/PITFALLS.md"
  improvementLog: ".harness/improvement-log.md"
